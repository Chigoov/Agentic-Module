# AUTONOMI AGENTIC ILMIAH — Phase 1 (Foundation) dependencies
# Pinned to the versions verified in this environment. See pyproject.toml.

pydantic==2.13.4
PyYAML==6.0.3
python-dotenv==1.2.2

# --- test tooling ---
pytest==9.1.1
