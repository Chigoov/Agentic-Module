# AUTONOMI AGENTIC ILMIAH — Phase 1 (Foundation) dependencies
# Versions are pinned to the exact versions verified in this environment
# (Python 3.14.5, Windows). See docs/PHASE_1_REPORT.md.

pydantic==2.13.4
PyYAML==6.0.3
python-dotenv==1.2.2

# --- test tooling ---
pytest==9.1.1
